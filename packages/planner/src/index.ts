import { Task, Epic } from "@tencoder/core";

export class Planner {
  generateTasks(_prd: string, _repoSignals: unknown[]): Task[] {
    // Placeholder implementation
    return [];
  }

  generateEpics(_tasks: Task[]): Epic[] {
    // Placeholder implementation
    return [];
  }
}
